from typing import List


class Solution:
    def largest(self, n : int, arr : List[int]) -> int:
        # code here
        largest = arr[0]
        #for(i=1;i<n;i++):
        for i in range(1,n):
            if(arr[i]>largest):
                largest = arr[i]
        return largest
